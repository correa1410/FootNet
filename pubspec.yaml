name: footnet
description: A new Flutter project.

publish_to: 'none' 

version: 0.0.1+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  webview_flutter: ^3.0.4
  firebase_core: ^3.1.0
  firebase_auth: ^5.1.0
  provider: ^6.0.0
  http: ^1.2.1
  xml: ^6.1.0
  url_launcher: ^6.0.20
  intl: ^0.19.0
  cloud_firestore: ^5.0.1
  flutter_launcher_icons: ^0.14.1
  shared_preferences: ^2.0.0
  html: ^0.15.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter_icons:
  android: true
  ios: true
  image_path: "lib/images/Logo.png"
flutter:
  uses-material-design: true
  assets:
    - assets/api-football.css